function [lower,upper]=rootrange(f,x)
n=size(x,2);
for i=2:n
    value=f(x(i))*f(x(i+1));
    if value < 0
         lower=x(i-1);
         upper=x(i);
         return;
    end
    if f(x(i)) == 0
        lower=x(i);
        upper=x(i-1);
    end
    if f(x[i-1]==0
end
fprintf('\nThe function is continously increasing or decreasing !!\n');
end
